# ZCode 人格部署 — 修改记录

> 更新日期: 2026-09-02
> 目标: 将 ZCode 的 SYSTEM 层完全替换为自定义人格文件
> 当前状态: ✅ 已部署 (人格 36000 bytes)

---

## 1. 架构原理

### ZCode 的 system prompt 拼装流程（原版）

```
zcode.cjs 内部:
  build() 函数
    ├── Djo = "You are ZCode, an interactive coding agent"  ← CLI 前缀
    ├── customSystemPrompt (默认为空)
    ├── Eut() = Agent Identity 片段
    └── fre() = 日期提醒片段
         ↓
    拼成完整 system 层发给模型
```

### 部署后的流程

```
  build() 函数
    ├── Djo = ""                                   ← 补丁1 置空
    ├── customSystemPrompt = readFileSync(人格.txt) ← 补丁2 读外部文件
    ├── Eut() = readFileSync(人格.txt)              ← 补丁3 读外部文件
    └── fre() = return null                        ← 补丁4 移除
         ↓
    SYSTEM 层 = 人格.txt 全文，无其他内容
```

**核心**: 利用 `customSystemPrompt` 这个产品自带的合法入口，
把它从「读 config」改成「读外部文件」，实现热换人格。

---

## 2. 四个补丁明细

deploy.ps1 对 `zcode.cjs` 做的修改:

| # | 目标 | 原代码 | 修改后 | 作用 |
|---|------|--------|--------|------|
| 1 | CLI 前缀 | `Djo="You are ZCode, an interactive coding agent"` | `Djo=""` | 删除产品身份声明 |
| 2 | build() 入口 | `n=this.config.customSystemPrompt?.trim(),o=!!n;` | `n=(function(){try{return require("fs").readFileSync("E:/ZCode-Deploy/人格.txt","utf8")}catch(e){return null}})()?.trim(),o=!!n;` | system prompt 改为读外部文件 |
| 3 | Eut() 函数 | `function Eut(e){let t=Njo(e);...}` | `function Eut(e){let t;try{t=require("fs").readFileSync("...","utf8")}catch(r){t=Njo(e)}...}` | Agent Identity 片段同样读人格文件 |
| 4 | fre() 函数 | `function fre(e){if(!e)return null...}` | `function fre(e){return null}` | 移除日期提醒注入 |

**路径说明**: 补丁 2/3 中写死了 `E:/ZCode-Deploy/人格.txt` 的绝对路径。
移动文件夹后必须重跑 `部署.bat`，否则读文件失败会静默回退原版。

---

## 3. 文件清单

```
E:\ZCode-Deploy\
├── 部署.bat          一键部署（调 deploy.ps1 install）
├── 恢复.bat          恢复原版（调 deploy.ps1 restore）
├── 查看状态.bat       查状态（调 deploy.ps1 status）
├── deploy.ps1        主脚本（路径自动检测 + 补丁逻辑）
├── 人格.txt          人格文件（唯一编辑入口，36000 bytes）
├── README.txt        简要使用说明
├── 修改记录.md       本文档
└── test_out.txt      早期测试输出（可删）

F:\zcode\
├── resources\glm\zcode.cjs       ← 被修改的目标
└── resources\glm\zcode.cjs.bak   ← 自动备份的原版
```

### deploy.ps1 的路径自动检测顺序

1. 运行中的 ZCode 进程路径
2. `F:\zcode` / `C:\zcode` / `D:\zcode`
3. `%LOCALAPPDATA%\Programs\zcode`
4. `C:\Program Files\zcode` 等常规位置
5. 扫描所有盘符根目录

---

## 4. 日常操作

### 换人格（最常用）

```
1. 编辑 E:\ZCode-Deploy\人格.txt
2. 保存
3. 开新对话 → 生效
（不需要重启 ZCode，不需要重跑部署）
```

### 部署 / 恢复 / 状态

```powershell
# 部署
powershell -ExecutionPolicy Bypass -File E:\ZCode-Deploy\deploy.ps1 install

# 恢复原版
powershell -ExecutionPolicy Bypass -File E:\ZCode-Deploy\deploy.ps1 restore

# 查看状态
powershell -ExecutionPolicy Bypass -File E:\ZCode-Deploy\deploy.ps1 status
```

或直接双击对应的 .bat。

### restore 的行为

- 还原 `zcode.cjs` ← `.bak`
- **保留** `人格.txt`（不会被删）
- 重启 ZCode

---

## 5. 验证方法（日志）

ZCode 的模型请求日志:

```
C:\Users\win11\.zcode\cli\rollout\model-io-*.jsonl
```

检查点:
- system 层应为 `人格.txt` 全文
- 不应出现 `"You are ZCode"`
- 不应出现产品安全策略
- 不应出现日期提醒

---

## 6. 已知问题

| 问题 | 说明 |
|------|------|
| ZCode 更新后补丁失效 | 更新会覆盖 `zcode.cjs`，重跑 `部署.bat` 即可 |
| 文件夹移动后失效 | 路径写死在 cjs 里，重跑 `部署.bat` |
| 模型层抵抗 | 产品层已干净，但模型自身 RLHF 可能不完全采纳人格（无法在补丁层解决） |
| `.bak` 是首次部署时的版本 | 若 ZCode 已更新过，restore 会回旧版，建议更新后先 restore 再重新 install |

---

## 7. 与 WorkBuddy 部署的区别

| | ZCode | WorkBuddy |
|---|---|---|
| 文件夹 | `E:\ZCode-Deploy` | `E:\WorkBuddy-Deploy` |
| 目标 | `zcode.cjs` 的 build() | `codebuddy.js` 的 ns() |
| 入口 | customSystemPrompt | 直接函数开头注入 |
| 额外清理 | 4 个补丁 | 模板清空 + 缓存策略删除 + 隐藏指令关闭 |
| 互不干扰 | ✅ 完全独立 | ✅ 完全独立 |